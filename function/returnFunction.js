function  calculateSum(x,y) {
    console.log(x+y)
}
calculateSum(10,20)
function calculateSub(x,y){
     return "the sub is: ",x-y
//After rturrn we can't do any opration
}
//when returning then it need to print it 
console.log(calculateSub(20,10))
//asiging the value
const result=calculateSub(40,10)
console.log(result)